<form method="post" action="{{ path('signin_delete', {'id': signin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signin.id) }}">
    <button class="btn">Delete</button>
</form>
